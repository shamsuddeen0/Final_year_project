we should not store password itself 
not plaintexty
not encrypted 

1. we should use a strong hash algorithm 
--bcypt/Argon2
--design it to be slow to prevent bruteforce 

2. always add unique salt
--random value per user
--stored along with the hash 
--it prevents rainbow table attacks 

3. on login
--Hash (password + salt)
--compare with stored hash 
--never decrypt anything 

4. extra protection (production system )
--rate limiting on login
--account lock after failures 
--optional pepper (server-side secret)

if the database leakes attackers cannot recover the password
